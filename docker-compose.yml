version: '3'

services:
  apache:
    build:
        context: .
        dockerfile: Dockerfile
    container_name: portfolio
    ports:
      - '1234:80'
    volumes:
      - ./dist:/app
    restart: always